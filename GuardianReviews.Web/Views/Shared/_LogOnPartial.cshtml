@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Context.User.Identity.Name</b>
    [ @Html.ActionLink("Log Off", "LogOut", "User") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogIn", "User") ]
}
